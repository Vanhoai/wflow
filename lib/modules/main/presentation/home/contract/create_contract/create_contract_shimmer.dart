import 'package:flutter/material.dart';

class CreateContractShimmer extends StatelessWidget {
  const CreateContractShimmer({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
