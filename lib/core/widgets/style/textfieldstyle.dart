

import 'package:flutter/material.dart';

TextTitle({double size =14, Color colors = Colors.black,FontWeight fontWeight = FontWeight.w300}) {
  return TextStyle(

    fontSize: size,
    fontWeight: fontWeight,
    color: colors,
  );
}